#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;
using vi = vector<int>;
using vll = vector<ll>;

constexpr int MOD = 1e9 + 7;
constexpr ll INF = 1e18;

#define all(x) begin(x), end(x)
#define rall(x) rbegin(x), rend(x)
#define pb push_back
#define mp make_pair
#define sz(x) (int)(x).size()

string append(string s, string x){
    
}

void solve() {
    int n;
    cin >> n;
    int m;
    cin >> n;
    string x;
    cin >> x;
    string s;
    cin >> s;

    // operations for s to be in x

    // we just check last m window in x
    while (true){
        if (s.length() > x.length()){

        }
    }

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1; 
    cin >> t;
    
    while (t--) {
        solve();
    }
    
    return 0;
}